<VirtualHost *:*>
    ServerName _
        
    SSLProxyEngine On
    SSLProxyCheckPeerCN off
    SSLProxyCheckPeerName off
    SSLProxyCheckPeerExpire off
    SSLProxyVerify none

    ProxyPass / ${VM_IP_ADDRESS}
    ProxyPassReverse / ${VM_IP_ADDRESS}
</VirtualHost>